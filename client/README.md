= Building =

```bash
# Install dependencies with npm:
$ npm install
# Run the build with npm run (installs index.html and bundled resources into ../static)
$ npm run build
# Clean with npm run:
$ npm run clean
```
